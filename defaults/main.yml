---
# defaults file for weblogic-deploy-tooling
user: "oracle"
group: "oracle"
github_wdt_base_url: "https://api.github.com/repos/oracle/weblogic-deploy-tooling/releases"
wdt_version: "latest"
dest_dir: "/admin/utils"
wdt_path: "{{ dest_dir }}/weblogic-deploy" # resulting directory after unarchive operation
